{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@10.9.2",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "turbo": "^2.5.2"
  }
}
